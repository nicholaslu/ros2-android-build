diff --git a/rcljava/CMakeLists.txt b/rcljava/CMakeLists.txt
index adf0512..83c1a56 100644
--- a/rcljava/CMakeLists.txt
+++ b/rcljava/CMakeLists.txt
@@ -12,6 +12,10 @@ find_package(rcl_interfaces REQUIRED)
 find_package(rcljava_common REQUIRED)
 find_package(rmw REQUIRED)
 find_package(rmw_implementation_cmake REQUIRED)
+# Provides string_camel_case_to_lower_case_underscore(), used below. On Humble this
+# arrived transitively via the interface packages; on Jazzy those depend on
+# rosidl_core_* instead, so rosidl_cmake's extras no longer get included here.
+find_package(rosidl_cmake REQUIRED)
 
 include(CrossCompilingExtra)
 
@@ -39,9 +43,30 @@ set(CMAKE_JAVA_COMPILE_FLAGS "-source" "1.6" "-target" "1.6")
 
 include_directories(include)
 
+# ament_target_dependencies() was removed in Lyrical (ament_cmake 2.x), but the
+# per-package <pkg>_INCLUDE_DIRS / <pkg>_LIBRARIES / <pkg>_DEFINITIONS variables it
+# consumed are still exported by the ament_cmake_export_* extras. Apply them directly.
+if(NOT COMMAND _rcljava_target_dependencies)
+  function(_rcljava_target_dependencies _target)
+    foreach(_pkg ${ARGN})
+      if(NOT ${_pkg}_FOUND)
+        find_package(${_pkg} QUIET REQUIRED)
+      endif()
+      if(${_pkg}_INCLUDE_DIRS)
+        target_include_directories(${_target} PUBLIC ${${_pkg}_INCLUDE_DIRS})
+      endif()
+      if(${_pkg}_LIBRARIES)
+        target_link_libraries(${_target} ${${_pkg}_LIBRARIES})
+      endif()
+      if(${_pkg}_DEFINITIONS)
+        target_compile_definitions(${_target} PUBLIC ${${_pkg}_DEFINITIONS})
+      endif()
+    endforeach()
+  endfunction()
+endif()
+
 function(set_properties _target_name _build_type)
   set_target_properties(${_target_name} PROPERTIES
-    PREFIX ""
     LIBRARY_OUTPUT_DIRECTORY${_build_type} "${CMAKE_CURRENT_BINARY_DIR}/${PROJECT_NAME}"
     RUNTIME_OUTPUT_DIRECTORY${_build_type} "${CMAKE_CURRENT_BINARY_DIR}/${PROJECT_NAME}"
   )
@@ -93,7 +118,8 @@ foreach(_jni_source ${${PROJECT_NAME}_jni_sources})
     set(_jni_prefix "lib")
   endif()
   set_target_properties(${_target_name} PROPERTIES
-    OUTPUT_NAME "${_jni_prefix}${_target_name}__jni"
+    PREFIX "${_jni_prefix}"
+    OUTPUT_NAME "${_target_name}__jni"
   )
 
   set_properties("${_target_name}" "")
@@ -104,7 +130,7 @@ foreach(_jni_source ${${PROJECT_NAME}_jni_sources})
     set_properties("${_target_name}" "_RELWITHDEBINFO")
   endif()
 
-  ament_target_dependencies(${_target_name}
+  _rcljava_target_dependencies(${_target_name}
     "builtin_interfaces"
     "rcl"
     "rcljava_common"
diff --git a/rcljava/package.xml b/rcljava/package.xml
index 0ca7574..869faf2 100644
--- a/rcljava/package.xml
+++ b/rcljava/package.xml
@@ -11,6 +11,7 @@
   <buildtool_depend>ament_cmake_export_jars</buildtool_depend>
   <buildtool_depend>ament_cmake_export_jni_libraries</buildtool_depend>
   <buildtool_depend>rcljava_common</buildtool_depend>
+  <buildtool_depend>rosidl_cmake</buildtool_depend>
 
   <build_depend>builtin_interfaces</build_depend>
   <build_depend>rcl_interfaces</build_depend>
diff --git a/rcljava/src/main/cpp/org_ros2_rcljava_executors_BaseExecutor.cpp b/rcljava/src/main/cpp/org_ros2_rcljava_executors_BaseExecutor.cpp
index 287a175..33c0607 100644
--- a/rcljava/src/main/cpp/org_ros2_rcljava_executors_BaseExecutor.cpp
+++ b/rcljava/src/main/cpp/org_ros2_rcljava_executors_BaseExecutor.cpp
@@ -52,7 +52,9 @@ convert_rmw_request_id_to_java(JNIEnv * env, rmw_request_id_t * request_id)
   assert(jsequence_number_field_id != nullptr);
   assert(jwriter_guid_field_id != nullptr);
 
-  int8_t * writer_guid = request_id->writer_guid;
+  // rmw_request_id_t::writer_guid is uint8_t[] as of Jazzy (rmw 7.x); it was
+  // int8_t[] in Humble.
+  uint8_t * writer_guid = request_id->writer_guid;
   int64_t sequence_number = request_id->sequence_number;
 
   env->SetLongField(jrequest_id, jsequence_number_field_id, sequence_number);
@@ -82,7 +84,9 @@ convert_rmw_request_id_from_java(JNIEnv * env, jobject jrequest_id)
 
   rmw_request_id_t * request_id = static_cast<rmw_request_id_t *>(malloc(sizeof(rmw_request_id_t)));
 
-  int8_t * writer_guid = request_id->writer_guid;
+  // rmw_request_id_t::writer_guid is uint8_t[] as of Jazzy (rmw 7.x); it was
+  // int8_t[] in Humble.
+  uint8_t * writer_guid = request_id->writer_guid;
   request_id->sequence_number = env->GetLongField(jrequest_id, jsequence_number_field_id);
 
   jsize writer_guid_len = 16;  // See rmw/rmw/include/rmw/types.h
diff --git a/rcljava/src/main/cpp/org_ros2_rcljava_node_NodeImpl.cpp b/rcljava/src/main/cpp/org_ros2_rcljava_node_NodeImpl.cpp
index a7c662f..1b1784b 100644
--- a/rcljava/src/main/cpp/org_ros2_rcljava_node_NodeImpl.cpp
+++ b/rcljava/src/main/cpp/org_ros2_rcljava_node_NodeImpl.cpp
@@ -228,8 +228,10 @@ Java_org_ros2_rcljava_node_NodeImpl_nativeCreateTimerHandle(
   rcl_timer_t * timer = static_cast<rcl_timer_t *>(malloc(sizeof(rcl_timer_t)));
   *timer = rcl_get_zero_initialized_timer();
 
-  rcl_ret_t ret = rcl_timer_init(
-    timer, clock, context, timer_period, NULL, rcl_get_default_allocator());
+  // rcl_timer_init() was removed in Lyrical (rcl 10.x). rcl_timer_init2() is the
+  // replacement; the trailing autostart=true reproduces the old behaviour.
+  rcl_ret_t ret = rcl_timer_init2(
+    timer, clock, context, timer_period, NULL, rcl_get_default_allocator(), true);
 
   if (ret != RCL_RET_OK) {
     std::string msg = "Failed to create timer: " + std::string(rcl_get_error_string().str);
diff --git a/rcljava_common/src/main/java/org/ros2/rcljava/common/JNIUtils.java b/rcljava_common/src/main/java/org/ros2/rcljava/common/JNIUtils.java
index 58661a7..b5e0f11 100644
--- a/rcljava_common/src/main/java/org/ros2/rcljava/common/JNIUtils.java
+++ b/rcljava_common/src/main/java/org/ros2/rcljava/common/JNIUtils.java
@@ -23,6 +23,19 @@ public final class JNIUtils {
 
   private static final String TYPESUPPORT = "rosidl_typesupport_c";
 
+  // Typesupport-implementation-specific JNI libraries. Each contains its own copy of the
+  // generated convert_from_java/convert_to_java functions, whose cached jclass/jmethodID
+  // globals are only initialized in JNI_OnLoad. These libraries are normally pulled in as
+  // ELF dependencies of other JNI libraries or dlopen()ed by the rosidl_typesupport_c
+  // dispatcher, neither of which triggers JNI_OnLoad, so the JVM/ART may bind a native
+  // method to a copy whose globals are still null (crashing with "GetFieldID received
+  // NULL jclass"). Loading them explicitly via System.loadLibrary makes JNI_OnLoad run
+  // for every copy, so any binding is safe.
+  private static final String[] TYPESUPPORT_IMPLEMENTATIONS = {
+    "rosidl_typesupport_fastrtps_c",
+    "rosidl_typesupport_introspection_c",
+  };
+
   /**
    * Private constructor so this cannot be instantiated.
    */
@@ -52,5 +65,14 @@ public final class JNIUtils {
     libraryName = libraryName + "__jni__" + JNIUtils.TYPESUPPORT;
     logger.info("Loading typesupport: " + libraryName);
     System.loadLibrary(libraryName);
+    for (String implementation : TYPESUPPORT_IMPLEMENTATIONS) {
+      String implementationLibraryName =
+          normalizeClassName(cls) + "__jni__" + implementation;
+      try {
+        System.loadLibrary(implementationLibraryName);
+      } catch (UnsatisfiedLinkError e) {
+        logger.info("Typesupport implementation not available: " + implementationLibraryName);
+      }
+    }
   }
 }
diff --git a/rosidl_generator_java/cmake/rosidl_generator_java_generate_interfaces.cmake b/rosidl_generator_java/cmake/rosidl_generator_java_generate_interfaces.cmake
index 0093329..4744057 100644
--- a/rosidl_generator_java/cmake/rosidl_generator_java_generate_interfaces.cmake
+++ b/rosidl_generator_java/cmake/rosidl_generator_java_generate_interfaces.cmake
@@ -206,6 +206,29 @@ macro(set_properties _build_type)
     OUTPUT_NAME "${_library_path}")
 endmacro()
 
+# ament_target_dependencies() was removed in Lyrical (ament_cmake 2.x), but the
+# per-package <pkg>_INCLUDE_DIRS / <pkg>_LIBRARIES / <pkg>_DEFINITIONS variables it
+# consumed are still exported by the ament_cmake_export_* extras. Apply them
+# directly so both call sites below keep working.
+if(NOT COMMAND _rcljava_target_dependencies)
+  function(_rcljava_target_dependencies _target)
+    foreach(_pkg ${ARGN})
+      if(NOT ${_pkg}_FOUND)
+        find_package(${_pkg} QUIET REQUIRED)
+      endif()
+      if(${_pkg}_INCLUDE_DIRS)
+        target_include_directories(${_target} PUBLIC ${${_pkg}_INCLUDE_DIRS})
+      endif()
+      if(${_pkg}_LIBRARIES)
+        target_link_libraries(${_target} ${${_pkg}_LIBRARIES})
+      endif()
+      if(${_pkg}_DEFINITIONS)
+        target_compile_definitions(${_target} PUBLIC ${${_pkg}_DEFINITIONS})
+      endif()
+    endforeach()
+  endfunction()
+endif()
+
 foreach(_generated_cpp_file ${_generated_extension_files})
   list(FIND _generated_extension_files ${_generated_cpp_file} _file_index)
   list(GET _typesupport_by_generated_cpp_files ${_file_index} _typesupport_impl)
@@ -256,15 +279,19 @@ foreach(_generated_cpp_file ${_generated_extension_files})
     ${rosidl_generate_interfaces_TARGET}__rosidl_generator_c
     ${_extension_link_flags}
   )
-  rosidl_target_interfaces(${_library_name}
-    ${PROJECT_NAME} rosidl_typesupport_c)
+  # rosidl_target_interfaces() was removed in Lyrical (rosidl 5.x); it was already
+  # deprecated in Jazzy. The replacement resolves the typesupport target by name,
+  # and linking it also propagates the generated interface include dirs.
+  rosidl_get_typesupport_target(_java_typesupport_target
+    "${PROJECT_NAME}" "rosidl_typesupport_c")
+  target_link_libraries(${_library_name} "${_java_typesupport_target}")
   target_include_directories(${_library_name}
     PUBLIC
     ${CMAKE_CURRENT_BINARY_DIR}/rosidl_generator_c
     ${CMAKE_CURRENT_BINARY_DIR}/rosidl_generator_java
     ${JNI_INCLUDE_DIRS}
   )
-  ament_target_dependencies(${_library_name}
+  _rcljava_target_dependencies(${_library_name}
     "rosidl_generator_c"
     "rosidl_generator_java"
     "rcljava_common"
@@ -272,7 +299,7 @@ foreach(_generated_cpp_file ${_generated_extension_files})
     "rosidl_typesupport_interface"
   )
   foreach(_pkg_name ${rosidl_generate_interfaces_DEPENDENCY_PACKAGE_NAMES})
-    ament_target_dependencies(${_library_name}
+    _rcljava_target_dependencies(${_library_name}
       ${_pkg_name}
     )
     target_link_libraries(${_library_name} ${${_pkg_name}_JNI_LIBRARIES})
