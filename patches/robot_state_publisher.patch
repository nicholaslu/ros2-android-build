diff --git a/CMakeLists.txt b/CMakeLists.txt
index ad31bb3..92ba13a 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -15,6 +15,10 @@ find_package(ament_cmake REQUIRED)
 find_package(builtin_interfaces REQUIRED)
 find_package(geometry_msgs REQUIRED)
 find_package(kdl_parser REQUIRED)
+# Lyrical expects orocos_kdl from the system (liborocos-kdl-dev via rosdep), which
+# does not exist for an Android cross build. We still vendor it, so pull in the
+# vendor package first -- its -extras.cmake registers the vendored prefix.
+find_package(orocos_kdl_vendor REQUIRED)
 find_package(orocos_kdl REQUIRED)
 find_package(rcl_interfaces REQUIRED)
 find_package(rclcpp REQUIRED)
diff --git a/package.xml b/package.xml
index bba3863..3ae8ddd 100644
--- a/package.xml
+++ b/package.xml
@@ -29,6 +29,7 @@
 
   <exec_depend>liborocos-kdl</exec_depend>
 
+  <depend>orocos_kdl_vendor</depend>
   <build_depend>liborocos-kdl-dev</build_depend>
 
   <build_export_depend>liborocos-kdl-dev</build_export_depend>
