diff --git a/CMakeLists.txt b/CMakeLists.txt
index 36a7cbb..d325031 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -19,6 +19,10 @@ ament_vendor(libyaml_vendor
   VCS_VERSION 0.2.5
   CMAKE_ARGS
     "-DCMAKE_C_FLAGS:STRING=${CMAKE_C_FLAGS}$<$<BOOL:${MSVC}>: /wd4244 /wd4267 /wd4996>"
+    # libyaml 0.2.5 declares cmake_minimum_required(VERSION < 3.5), which CMake 4
+    # refuses outright. orocos_kdl_vendor passes the same flag upstream for the
+    # same reason.
+    -DCMAKE_POLICY_VERSION_MINIMUM=3.5
 )
 
 ament_export_dependencies(yaml)
